/** Single image blob — each upload overwrites. */
export const BLOB_PATH = 'camera-poc/latest';

/** JSON metadata for the last upload — each upload overwrites. */
export const META_PATH = 'camera-poc/meta.json';

/** Allowed location labels — edited via POST /api/locations (POC: unauthenticated). */
export const LOCATIONS_PATH = 'camera-poc/locations.json';
